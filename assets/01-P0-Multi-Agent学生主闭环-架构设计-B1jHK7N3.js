const n=`# P0 Multi-Agent 学生主闭环架构设计（AI教师版）

> 版本：v1.0  
> 文档属性：阶段架构版（P0）  
> 目标：只讲一期必交付主架构  
> 主线能力：\`Multi-Agent + 工作流编排 + 知识库 + 长期记忆 + 官方发布链接\`

---

## 目录

1. 一页结论
2. P0 目标与范围边界
3. P0 系统上下文图
4. P0 Agent 编排图
5. P0 知识库、变量与记忆流转图
6. P0 运行流程图
7. P0 发布与访问图
8. P0 异常兜底图
9. 组件职责表
10. 验收与演示建议
11. 官方依据

---

## 1. 一页结论

\`P0\` 只回答一个问题：

\`怎么先把 AI 教师学生主闭环稳定跑通。\`

这一阶段不追求大而全，只追求三件事：

- 学生提问后，系统能稳定完成 \`诊断 -> 讲解 -> 练习 -> 测评 -> 复盘\`。
- 回答基于课程资料，而不是通用闲聊。
- 发布后评委可以通过 ADP 官方入口实际体验。

P0 的架构关键词：

- \`TeacherOrchestrator\`
- \`DiagnosisAgent\`
- \`ExplanationAgent\`
- \`PracticeEvalAgent\`
- \`ReviewPlanAgent\`
- \`知识库\`
- \`visitor_biz_id\`
- \`custom_variables\`
- \`SYS.Memory\`

---

## 2. P0 目标与范围边界

### 2.1 P0 要做什么

- 跑通学生侧主闭环。
- 建立课程知识对齐能力。
- 建立长期记忆和课程隔离能力。
- 提供可访问发布版本。

### 2.2 P0 不做什么

- 不把 \`TeacherOpsAgent\` 写进阻塞主链路。
- 不把教师轻看板当成一期前提。
- 不引入产品后端、Redis、MQ。
- 不要求自定义前端才能演示。

---

## 3. P0 系统上下文图（图 1）

\`\`\`mermaid
flowchart LR
    S["学生"] --> E["ADP 已发布应用"]
    S --> I["图片 / 文本 / 语音 / 文档输入"]
    I --> E

    E --> W["Multi-Agent 工作流"]
    W --> K["课程知识库"]
    W --> M["SYS.Memory"]
    E --> P["官方发布链接 / 分享链接"]
\`\`\`

这张图想说明什么：

- P0 的核心系统边界完全可以放在 ADP 内完成。
- 学生通过平台入口发起请求，不需要先自建前端。
- 知识库和长期记忆是主闭环的关键底座。

---

## 4. P0 Agent 编排图（图 2）

\`\`\`mermaid
flowchart TD
    A["学生请求"] --> B["TeacherOrchestrator"]
    B --> C["DiagnosisAgent"]
    C --> D["ExplanationAgent"]
    D --> E["PracticeEvalAgent"]
    E --> F{"是否达标"}
    F -->|否| D
    F -->|是| G["ReviewPlanAgent"]
    G --> H["主控汇总并回复学生"]
\`\`\`

这张图想说明什么：

- \`TeacherOrchestrator\` 是调度入口，但真正的教学环节由子 Agent 分工完成。
- 主链路不是一问一答，而是一条可循环的教学流程。
- 如果学生没达标，允许返回讲解或继续练习。

### 4.1 为什么 P0 不让 \`TeacherOpsAgent\` 进主链路

- 因为 P0 的目标是先保证学生侧闭环稳定。
- 教师运营属于增强能力，应该在主闭环稳定后再叠加。
- 所以 P0 文档里只把 \`TeacherOpsAgent\` 视为后续阶段预留。

---

## 5. P0 知识库、变量与记忆流转图（图 3）

\`\`\`mermaid
flowchart LR
    U["学生请求"] --> A["统一上下文字段"]
    A --> A1["visitor_biz_id"]
    A --> A2["course_id"]
    A --> A3["class_id"]
    A --> A4["chapter_id"]
    A --> A5["role"]

    A --> W["P0 工作流"]
    W --> G["各教学 Agent"]
    G --> K["课程知识库检索"]
    G --> M["SYS.Memory 命中"]
    G --> O["主控汇总输出"]
\`\`\`

这张图想说明什么：

- P0 的连续学习能力依赖 \`visitor_biz_id\`。
- P0 的课程隔离能力依赖 \`course_id / class_id / chapter_id / role\`。
- 这些字段不会只在接口层用一次，而是会贯穿工作流、检索、记忆三处。

### 5.1 P0 固定字段约定

| 字段 | 用途 | 是否一期必须 |
| --- | --- | --- |
| \`visitor_biz_id\` | 终端学生唯一标识 | 是 |
| \`course_id\` | 课程边界控制 | 是 |
| \`class_id\` | 班级边界控制 | 建议 |
| \`chapter_id\` | 章节边界控制 | 建议 |
| \`role\` | 学生/教师角色识别 | 建议 |

---

## 6. P0 运行流程图（图 4）

\`\`\`mermaid
flowchart TD
    A["学生提问 / 拍题 / 上传资料"] --> B["多模态解析"]
    B --> C["TeacherOrchestrator 判断任务类型"]
    C --> D["DiagnosisAgent 学习诊断"]
    D --> E["ExplanationAgent 分层讲解"]
    E --> F["PracticeEvalAgent 引导练习与测评"]
    F --> G["ReviewPlanAgent 错因归因与学习计划"]
    G --> H["长期记忆更新"]
    H --> I["主控输出最终回复"]
\`\`\`

这张图想说明什么：

- P0 不是“先聊天再看情况”，而是固定教学闭环。
- 多模态解析只是入口，重点在后续的编排和教学输出。
- 最终输出不是单个 Agent 的原始结果，而是主控收束后的学生回复。

---

## 7. P0 发布与访问图（图 5）

\`\`\`mermaid
flowchart LR
    A["P0 配置完成"] --> B["应用发布"]
    B --> C["服务状态"]
    C --> D["立即体验"]
    C --> E["分享链接 / 二维码"]
    C --> F["API 管理 / AppKey"]
\`\`\`

这张图想说明什么：

- P0 默认访问方式就是 ADP 官方发布链接。
- 这条路最稳，也最适合比赛第一版。
- \`AppKey\` 在 P0 只需要“知道有”，不要求马上接自定义前端。

---

## 8. P0 异常兜底图（图 6）

\`\`\`mermaid
flowchart TD
    A["主链路执行中"] --> B{"异常类型"}
    B -->|低置信| C["降级为保守讲解 + 引导澄清"]
    B -->|检索不足| D["回退基础知识包 + 提示补充资料"]
    B -->|节点超时| E["工作流异常处理 / 重试"]
    B -->|整体不稳| F["退回 Standard 保底路线"]
    C --> G["保留主闭环"]
    D --> G
    E --> G
    F --> H["保留知识库 + 发布入口"]
\`\`\`

这张图想说明什么：

- P0 不是只设计成功路径，也要设计失败路径。
- 只要异常不致命，优先保持学生主闭环不中断。
- 如果主链路稳定性不够，允许退回 \`Standard\` 保住演示。

---

## 9. 组件职责表

| 组件 | 输入 | 输出 | 关键约束 |
| --- | --- | --- | --- |
| \`TeacherOrchestrator\` | 学生问题、历史上下文、变量、长期记忆 | 最终教学回复、任务调度结果 | 唯一学生入口 |
| \`DiagnosisAgent\` | 当前问题、课程标签、记忆摘要 | 水平判断、卡点、建议路径 | 不直接面向学生 |
| \`ExplanationAgent\` | 诊断结果、检索片段 | 分层讲解、步骤说明、例子 | 回答要贴课程资料 |
| \`PracticeEvalAgent\` | 讲解结果、题库、学生作答 | 练习题、评分、达标判断 | 支持未达标回流 |
| \`ReviewPlanAgent\` | 错题、评分、知识点标签 | 错因归因、学习计划、复盘建议 | 输出进入记忆更新 |
| \`知识库\` | PPT、讲义、题库、FAQ | 检索片段 | 必须做课程标签隔离 |
| \`SYS.Memory\` | 多轮对话摘要 | 学生连续学习上下文 | 依赖 \`visitor_biz_id\` |

---

## 10. 验收与演示建议

### 10.1 P0 验收门禁

| 项 | 通过标准 |
| --- | --- |
| 主闭环 | 能完整跑通 \`诊断 -> 讲解 -> 练习 -> 测评 -> 复盘\` |
| 知识对齐 | 回答能命中课程资料 |
| 多模态 | 支持文本 / 图片 / 语音 / 文档输入 |
| 记忆连续 | 同一学生第二轮提问能延续上下文 |
| 发布可用 | 评委可通过官方发布入口访问 |

### 10.2 P0 对应 FR 范围

| 范围 | 对应内容 |
| --- | --- |
| \`FR-01 ~ FR-08\` | AI 教师策略、多模态理解、学习诊断、分层讲解、引导练习、形成性测评、错因归因、个性化学习计划 |

### 10.3 P0 演示建议

- 演示 1 门课、1 个章节、1 条端到端链路最稳。
- 展示顺序建议：拍题/提问 -> 诊断 -> 讲解 -> 练习 -> 判题 -> 复盘 -> 第二轮追问。
- 如果现场时间紧，就优先展示“主闭环 + 发布链接 + 记忆连续”。

---

## 11. 官方依据

- 《什么是 Multi-Agent？》  
  https://cloud.tencent.com/document/product/1759/118325
- 《工作流编排》  
  https://cloud.tencent.com/document/product/1759/122556
- 《Agent 节点》  
  https://cloud.tencent.com/document/product/1759/122554
- 《知识检索相关设置》  
  https://cloud.tencent.com/document/product/1759/112704
- 《长期记忆说明》  
  https://cloud.tencent.com/document/product/1759/122458
- 《应用发布概述》  
  https://cloud.tencent.com/document/product/1759/104209

`;export{n as default};
