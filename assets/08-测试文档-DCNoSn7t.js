const n="# 比赛版测试文档\n\n## 1. 核心验收范围\n\n这份测试文档只覆盖当前比赛版最关键的四条主链：\n\n- 开学启动后一次拿到会话和第一版地图\n- 短诊断后能看到重规划结果\n- 提交答案后先返回反馈，再异步生成画像和笔记\n- 知识发布后生成新地图版本，回滚后恢复稳定版本\n\n## 2. 验收总表\n\n| 编号 | 场景 | 核心目标 |\n| --- | --- | --- |\n| TC-01 | 开学启动与首图生成 | 启动接口一次返回学习启动会话和第一版学习地图 |\n| TC-02 | 短诊断与重规划 | 诊断后生成短诊断结果、重规划事件和地图变化 |\n| TC-03 | 闯关作答与异步沉淀 | 作答后先拿反馈，再拿画像和笔记 |\n| TC-04 | 知识发布、回滚与地图版本更新 | 发布后生成新地图版本，回滚后恢复稳定版本 |\n\n## 3. TC-01 开学启动与首图生成\n\n| 项目 | 内容 |\n| --- | --- |\n| 前置条件 | 学生账号存在；`subject_catalog` 中至少有一个可选科目；ADP 与数据库可用 |\n| 操作步骤 | 1. 访问选科与开学页 2. 调用 `GET /api/startup/subjects` 3. 选择 `math` 4. 调用 `POST /api/startup/session` |\n| 预期接口结果 | 返回 `SubjectSelection`、`LearningSession`、`LearningMap`、`currentNodeId`、`recommendedNextNodeId` |\n| 预期数据库变化 | 新增 `subject_selections`、`subject_selection_items`、`learning_sessions`、`learning_maps`、`learning_map_stages`、`map_nodes`，并回填 `learning_sessions.current_map_id` |\n| 预期页面表现 | 页面直接跳到学习地图页，地图首节点和推荐下一步可见 |\n\n## 4. TC-02 短诊断与重规划\n\n| 项目 | 内容 |\n| --- | --- |\n| 前置条件 | 已存在一条有效 `LearningSession`；当前会话已有第一版学习地图 |\n| 操作步骤 | 1. 进入 AI 学习地图页 2. 填写短诊断 3. 调用 `POST /api/diagnostics/{subjectId}/submit` |\n| 预期接口结果 | 返回 `DiagnosticResult`、`RerouteEvent` 和更新后的 `LearningMap` |\n| 预期数据库变化 | 新增 `diagnostic_results`、`reroute_events`；受影响的 `learning_maps`、`map_nodes` 状态被更新 |\n| 预期页面表现 | 地图出现新的推荐节点或补桥节点，页面能解释“为什么这样调整” |\n\n## 5. TC-03 闯关作答与异步沉淀\n\n| 项目 | 内容 |\n| --- | --- |\n| 前置条件 | 已存在可进入的 `LearningTask`；流式通道可用 |\n| 操作步骤 | 1. 调用 `POST /api/learning/sessions` 创建学习任务会话 2. 订阅 `GET /api/learning/sessions/{sessionId}/stream` 3. 提交答案到 `POST /api/learning/sessions/{sessionId}/answers` 4. 稍后调用 `GET /api/learning/sessions/{sessionId}/feedback` |\n| 预期接口结果 | `/answers` 先返回 `CheckpointAttempt`、`FeedbackEvent`、`nextAction`、`settlementStatus=pending`；`/feedback` 最终返回 `LearnerProfileSnapshot`、`NoteBundle`、`storage_assets`，且 `settlementStatus=ready` |\n| 预期数据库变化 | 先写 `learning_task_sessions`、`checkpoint_attempts`、`feedback_events`；异步再写 `learner_profile_snapshots`、`note_bundles`、`storage_assets` |\n| 预期页面表现 | 闯关页立即显示评分和推进动作；成长页稍后能看到画像、导图和复习计划 |\n\n## 6. TC-04 知识发布、回滚与地图版本更新\n\n| 项目 | 内容 |\n| --- | --- |\n| 前置条件 | 已存在 `candidate` 状态的候选知识；至少有一个学习会话正在使用受影响学科 |\n| 操作步骤 | 1. 调用 `POST /api/knowledge/releases` 执行发布 2. 调用 `GET /api/knowledge/releases/{releaseId}` 查看结果 3. 触发问题场景后再次调用 `POST /api/knowledge/releases` 执行回滚 |\n| 预期接口结果 | 发布时返回 `KnowledgeRelease`、`StrategySnapshot`、受影响地图版本摘要；回滚时返回 `KnowledgeRollback`、恢复后的稳定版本信息 |\n| 预期数据库变化 | 发布时写 `validation_reports`、`knowledge_releases`、`strategy_snapshots`，并新增 `learning_maps.version_no + 1`；回滚时写 `knowledge_rollbacks`，并把受影响会话切回稳定版本或再生成稳定版本 |\n| 预期页面表现 | 后台能看到发布结果、影响范围、地图版本变化；回滚后显示“已恢复稳定版本” |\n\n## 7. 关键非功能检查\n\n| 检查项 | 验收标准 |\n| --- | --- |\n| 流式稳定性 | SSE 中断后前端能提示并重连，不直接白屏 |\n| 审计可追踪 | 重规划、发布、回滚、异常都能在 `audit_events` 找到记录 |\n| 资源索引完整 | 导图、导出物、上传资料都能在 `storage_assets` 查到索引 |\n| 中文可读性 | 页面提示、后台说明、接口文档以中文为主，评委可直接阅读 |\n\n## 8. 性能与稳定性验证\n\n### 8.1 性能 smoke\n\n| 验证对象 | 验证方式 | 通过标准 | 不通过时优先排查入口 |\n| --- | --- | --- | --- |\n| 首屏关键入口可见性 | 打开学生主线首屏和后台核心页，人工计时观察 | 3 秒内看到关键按钮或首个状态块 | 前端路由加载、首屏接口是否过重 |\n| 启动接口时延 | 调 `POST /api/startup/session` 记录接口返回时间 | 5 秒内拿到学习启动会话和第一版学习地图 | `startup_service`、ADP 起图耗时、数据库写入 |\n| 诊断接口时延 | 调 `POST /api/diagnostics/{subjectId}/submit` 记录返回时间 | 4 秒内拿到短诊断结果和重规划结果 | `diagnostic_service`、`map_service`、当前地图读取 |\n| SSE 首条事件时间 | 订阅 `GET /api/learning/sessions/{sessionId}/stream` 并记录首条事件到达时间 | 2 秒内收到首条 SSE 事件 | ADP 连通性、后端代理、前端 EventSource |\n| 作答即时反馈时间 | 调 `POST /api/learning/sessions/{sessionId}/answers` 记录返回时间 | 3 秒内拿到即时反馈 | `learning_session_service` 是否做了超范围同步工作 |\n| 异步沉淀完成时间 | 作答后轮询 `GET /api/learning/sessions/{sessionId}/feedback` | 10 秒内进入 `ready` 或明确 `failed` | `profile_note_service`、对象存储、资源索引写入 |\n\n### 8.2 稳定性检查\n\n| 验证对象 | 验证方式 | 通过标准 | 不通过时优先排查入口 |\n| --- | --- | --- | --- |\n| SSE 中断恢复 | 人为断开 SSE 或模拟超时 | 前端出现重连提示或降级提示，不直接卡死 | `learning_router`、前端流式层、`audit_events` |\n| 发布后地图版本局部更新 | 发布一个受影响候选知识后查看学习地图版本变化 | 只出现受影响会话的新地图版本，不出现整图异常抖动 | `strategy_service`、`map_service`、`learning_maps.version_no` |\n";export{n as default};
